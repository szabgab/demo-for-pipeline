pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'build'
                sh 'echo pwd'
                sh 'id'
                sh 'uname -a'
            }
        }
    }
}
