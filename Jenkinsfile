pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'build'
                sh 'echo build'
                sh 'pwd'
                sh 'id'
                sh 'uname -a'
            }
        }
    }
}
